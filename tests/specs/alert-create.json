[
  {
    "name": "Set Alert with empty watchlist shows empty state",
    "steps": [
      {
        "send": { "callback": "alert:create" },
        "expect": [
          { "method": "answerCallbackQuery" },
          {
            "method": "editMessageText",
            "payload": {
              "text": "No coins on your watchlist yet — add one before setting an alert.\nTap Manage Watchlist to get started."
            }
          }
        ]
      }
    ]
  }
]
